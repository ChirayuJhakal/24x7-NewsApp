<!DOCTYPE html>

<html lang="en">
  <head>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.5.2/css/bootstrap.min.css"
    />
    <style>
      table {
        width: "100%";
        border-collapse: collapse;
        /* margin-top: 20px; */
        margin: 70px;
      }

      th,
      td {
        border: 1px solid #ddd;
        padding: 12px;
        text-align: left;
      }

      th {
        background-color: #696969;
        color: #fff;
      }

      tr:nth-child(even) {
        background-color: #f2f2f2;
      }

      tr:hover {
        background-color: #ddd;
      }
    </style>
  </head>
  <body>
    <main>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
          <a class="navbar-brand fs-2">24x7 Admin</a>
          <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarNav"
            aria-controls="navbarNav"
            aria-expanded="false"
            aria-label="Toggle navigation"
          >
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link fs-4" aria-current="page" href="/addnews"
                  >NewsForm</a
                >
              </li>
              <li class="nav-item">
                <a class="nav-link active fs-4" href="/newslist">EditNews</a>
              </li>
              <li class="nav-item">
                <a class="nav-link fs-4" href="/logout"
                  ><button class="btn btn-danger">Logout</button>
                </a>
              </li>
            </ul>
          </div>
        </div>
      </nav>
      <p class="h1 text-center mt-5">News List</p>

      <% if(maindata != null) { %>
      <table>
        <thead>
          <tr>
            <th>Title</th>
            <th>Description</th>
            <th>Published At</th>
            <th>Delete</th>
          </tr>
        </thead>
        <tbody>
          <% for(let i = 0; i < maindata.length; i++) { %>
          <tr>
            <td><%= maindata[i].title %></td>
            <td><%= maindata[i].Description %></td>
            <td><%= maindata[i].published %></td>
            <td>
              <form action="/delete" method="post">
                <button
                  class="btn btn-danger"
                  value="<%= maindata[i]._id %>"
                  name="delete"
                >
                  Delete
                </button>
              </form>
            </td>
          </tr>
          <% } %>
        </tbody>
      </table>
      <% } %>
    </main>
  </body>
</html>
